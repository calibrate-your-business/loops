__pycache__/
*.pyc
.DS_Store

# Per-project profiles are private bindings (corporate specifics); the engine
# is public. Keep profiles local, or publish your own in your fork.
profiles/*

# EXCEPTION: plan-docs is a committed house standard (deliverable-type, not a private per-project binding).
!profiles/plan-docs.md
